DEFINE TABLE job SCHEMAFULL;
DEFINE FIELD id          ON job TYPE string;
DEFINE FIELD title       ON job TYPE string;
DEFINE FIELD level       ON job TYPE string;
DEFINE FIELD team        ON job TYPE string;
DEFINE FIELD location    ON job TYPE string;
DEFINE FIELD description ON job TYPE string;
